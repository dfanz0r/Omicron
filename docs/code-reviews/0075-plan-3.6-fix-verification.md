# Code Review 0075: Plan 3.6 Fix Verification

**Status:** Verified Complete  
**Date:** 2026-05-10  
**Source:** Review 0074 follow-up

---

## Changes Implemented

### 1. Transaction Lifecycle Event Tests (FH-0018 gap)

**File:** `Omicron.Core.Tests/WorkspaceTransactionTests.cs`

Added 5 tests:

| Test | Verifies |
|------|----------|
| `TransactionEvent_LazyStart_EmitsStartedOnce` | `TransactionStartedEvent` emitted exactly once on first stage |
| `TransactionEvent_PerOperation_EmitsStagedEvents` | Two `TransactionStagedEvent`s with correct paths and actions |
| `TransactionEvent_CommitSuccess_EmitsCommitted` | `TransactionCommittedEvent` present, no rollback event |
| `TransactionEvent_Rollback_EmitsRolledBack` | `TransactionRolledBackEvent` present, no commit event |
| `TransactionEvent_DisposeWithoutCommit_EmitsRolledBack` | Dispose after staging emits rollback event |

All tests construct `WorkspaceTransaction` with `InMemoryEventSink` and assert on event types/counts.

---

### 2. Commit Failure Recovery Tests (FH-0023 gap)

**File:** `Omicron.Core.Tests/WorkspaceTransactionTests.cs`

Added `FailingHostFileSystem` test double:
- Extends `HostWorkspaceFileSystem`
- Throws `IOException` after N successful write calls
- Delete/move operations bypass the failure counter (needed for rollback)

Added 2 tests:

| Test | Verifies |
|------|----------|
| `CommitFailure_MidCommit_RollsBackCompletedWrites` | 3 staged writes, 2nd fails → 1st is deleted, 2nd/3rd never written, rollback event emitted |
| `CommitFailure_Move_RollbackReversesMove` | Staged move + write, write fails → move is reversed, source restored, rollback event emitted |

Both tests verify `InvalidOperationException` with "rolled back" message, and assert on `TransactionRolledBackEvent` vs `TransactionCommittedEvent` presence.

---

### 3. Trivial Fixes

**a. `AgentSession._writer` made `readonly`**
- **File:** `Omicron.Core/Sessions/AgentSession.cs`
- Changed `private SessionEventWriter _writer;` → `private readonly SessionEventWriter _writer;`

**b. `LineEditor.EscapePressed` dead code removed**
- **File:** `Omicron.CLI/LineEditor.cs`
- Removed `public bool EscapePressed { get; private set; }` property
- Removed assignment `_engine.EscapePressed = true;` from `ReadLine()`
- `LineEditorState.EscapePressed` retained (used by engine and tested)

---

## Build & Test Results

```
Build:   0 warnings, 0 errors
Tests:   377 passing, 0 failed, 0 skipped
Delta:   +7 tests from 370 baseline
```

---

## Verdict

All review 0074 findings addressed. Plan 3.6 is complete and verified.
