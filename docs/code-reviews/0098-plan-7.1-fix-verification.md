# Code Review 0098: Plan 7.1 — Fix Verification

**Status:** Verified — All 9 findings resolved  
**Date:** 2026-05-10  
**Build:** 0 errors, 0 warnings (pre-existing xUnit analyzer warnings only)  
**Tests:** 657 pass (31 new), 0 failures

---

## Build & Test Results

```text
dotnet build → 0 errors, 0 CS/CA warnings
dotnet test  → 657 passing, 0 failed, 0 skipped
```

---

## Finding Verification (9/9)

### Finding 1 — P0 Critical: Streaming text invisible ✅ RESOLVED

**File:** `Omicron.CLI/Tui/TranscriptViewportWidget.cs`

**Fix verified:**
- `case AssistantTextDeltaEvent` now calls:
  1. `_pendingAssistantText.Append(delta.Delta)`
  2. `FlushPending()` — writes buffered text to store immediately
  3. `InvalidateLastBlock()` — re-wraps only the last block
  4. `RequestRender?.Invoke()` — triggers a frame render

- `RequestRender` callback is wired in `AppLayout` constructor:
  ```csharp
  _transcript.RequestRender = () => _shell.RequestRender();
  ```

**Result:** Assistant text now appears character-by-character during streaming, not just at completion.

---

### Finding 2 — P0 Critical: O(N²) style lookup ✅ RESOLVED

**File:** `Omicron.CLI/Tui/TranscriptViewportWidget.cs`

**Fix verified:**
- Added `private readonly Dictionary<BlockId, TranscriptBlock> _blockById = new();`
- `RebuildBlockIndex()` clears and rebuilds the dictionary from `_store.Blocks`
- Called from `RebuildLayout()` and `Arrange()`
- `GetStyleForBlock(BlockId)` now uses `_blockById.TryGetValue(blockId, out var block)` — O(1)

**Result:** Style lookup is constant time per visible line regardless of conversation length.

---

### Finding 3 — P0 Critical: StatusBarWidget CJK alignment ✅ RESOLVED

**File:** `Omicron.CLI/Tui/StatusBarWidget.cs`

**Fix verified:**
- Added `private static int GetDisplayWidth(string text)` using `CellWidthCalculator.GetWidth(Rune)` for each rune
- Center column: `(_bounds.Width - displayWidth) / 2`
- Right column: `_bounds.Right - displayWidth - 1`

**Result:** CJK status bar text is correctly centered and right-aligned.

---

### Finding 4 — P1 High: ViewportState follow-tail shows only last row ✅ RESOLVED

**File:** `Omicron.Core/Rendering/Transcript/ViewportState.cs`

**Fix verified:**
- `OnContentAppended(int newTotalWrappedRows, int viewportHeight = 1)` — uses `newTotalWrappedRows - viewportHeight`
- `ScrollToBottom(int totalWrappedRows, int viewportHeight = 1)` — uses `totalWrappedRows - viewportHeight`
- `UpdateTotalRows(int totalWrappedRows, int viewportHeight = 1)` — uses `totalWrappedRows - viewportHeight`

All three methods now accept `viewportHeight` and compute the first visible row as `total - height`.

**Result:** Follow-tail now shows the correct number of rows for the viewport height.

---

### Finding 5 — P1 High: Full reflow on every event ✅ RESOLVED

**File:** `Omicron.CLI/Tui/TranscriptViewportWidget.cs`

**Fix verified:**
- `InvalidateLastBlock()` — calls `_layout.InvalidateFrom(lastBlockId)` then `_layout.ReflowForWidth()` only for the invalidated region
- Called from `AssistantTextDeltaEvent` handler (streaming)
- `RebuildLayout()` (full reflow) is only called for discrete events:
  - `UserMessageEvent`
  - `AssistantResponseCompleteEvent`
  - `ToolInvocationStartedEvent`
  - `ToolInvocationCompletedEvent`
  - `SessionErrorEvent`

**Result:** Streaming deltas trigger incremental re-wrap of only the last block. Full reflow only happens on discrete events.

---

### Finding 6 — P1 High: InputEditorWidget cursor uses string index ✅ RESOLVED

**File:** `Omicron.CLI/Tui/InputEditorWidget.cs`

**Fix verified:**
- Added `GetDisplayWidth(string text)` helper using `CellWidthCalculator.GetWidth(Rune)`
- `cursorDisplayWidth = GetDisplayWidth(_buffer.ToString()[.._cursorPosition])`
- `cursorScreenCol = x + cursorDisplayWidth`

**Result:** Cursor is correctly positioned after CJK and emoji characters.

---

### Finding 7 — P2 Medium: Render assumes Y=0 ✅ RESOLVED

**File:** `Omicron.CLI/Tui/TranscriptViewportWidget.cs`

**Fix verified:**
- Text lines: `context.DrawText(_bounds.X, _bounds.Y + row, bytes.Span, style)`
- Unseen indicator: `context.DrawText(indicatorX, _bounds.Y + row - 1, ...)`

**Result:** Widget renders at its arranged bounds, not hardcoded at frame row 0.

---

### Finding 8 — P2 Medium: No per-widget clip contexts ✅ RESOLVED

**File:** `Omicron.Core/Rendering/Layout/VStack.cs`

**Fix verified:**
- Added `private readonly List<Rect> _childBounds = []`
- `Arrange()` stores each child's bounds in `_childBounds`
- `Render()` creates a clipped sub-context for each child:
  ```csharp
  var childContext = new RenderContext(
      context.Frame,
      context.Clip.Intersect(childRect),
      context.DefaultStyle);
  _children[i].Render(childContext);
  ```

**Result:** Children are clipped to their arranged bounds. Overflow is suppressed.

---

### Finding 9 — P2 Medium: Stale `_lastAssistantBlockId` ✅ RESOLVED

**File:** `Omicron.Core/Rendering/Transcript/TranscriptStore.cs`

**Fix verified:**
- `AppendUserMessage`: `_lastAssistantBlockId = null`
- `AppendToolCall`: `_lastAssistantBlockId = null`
- `AppendNotice`: `_lastAssistantBlockId = null`
- `CompleteLastAssistantBlock`: `_lastAssistantBlockId = null`

**Result:** Assistant block tracking is correctly reset on all non-assistant appends.

---

## Regression Checklist

- [x] `dotnet test` passes 657 tests (0 failures, 0 warnings)
- [x] Build has 0 CS/CA warnings
- [x] `TranscriptViewportWidget` flushes streaming text immediately
- [x] `GetStyleForBlock` uses dictionary lookup (O(1))
- [x] `StatusBarWidget` center/right alignment uses display width
- [x] `ViewportState` follow-tail accounts for viewport height
- [x] `InputEditorWidget` cursor positioned by display width
- [x] `TranscriptViewportWidget.Render` uses `_bounds.Y + row`
- [x] `VStack.Render` creates clipped sub-contexts per child
- [x] `TranscriptStore` clears `_lastAssistantBlockId` on non-assistant appends

---

## Verdict

**All 9 findings from Review 0097 are resolved. Plan 7.1 is verified and ready.**

**Proceed to Plan 8 (Markdown Parsing and Syntax Highlighting) or Plan 9 (Sandboxing Foundations).**
